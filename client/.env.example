# Client API Configuration
# For local development with Vite proxy, leave as /api or empty
# For separate domain / production deployment, set full URL (e.g. https://api.yourdomain.com/api)
VITE_API_URL=/api
